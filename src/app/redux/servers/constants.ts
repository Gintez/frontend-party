export const NAME = 'servers';

export const REQUEST_SERVERS = `${NAME}/REQUEST_SERVERS`;
export const SET_SERVERS = `${NAME}/SET_SERVERS`;
export const RESET_SERVERS_LISTS = `${NAME}/RESET_SERVERS_LISTS`;
